import 'package:flutter/material.dart';

class SearchePage extends StatelessWidget {
  const SearchePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
